<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
	<constant name="conststruts.devMode" value="true"/>
	<!-- Struts2的所有Action都需位于package下 -->
	<package name="lee" extends="struts-default">	    	   
	   <action name="*">
			<result>/WEB-INF/content/{1}.jsp</result>
		</action>	
	</package>
	
	<!--AJAX -->
	<package name="bps" extends="json-default">
		<action name="getAll_*" class="cn.edu.cumt.ec.action.UserBackAction" method="*"> 
    		<result name="success" type="json">
				<!-- 为该Result指定参数 -->
				<param name="noCache">true</param>
				<param name="contentType">text/html</param>
				<!-- 设置只序列Action的map属性 -->		
				<param name="root">returnpd</param>
			</result>
		</action> 	
	</package>	

		
</struts>